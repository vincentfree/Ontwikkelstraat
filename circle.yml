machine:
   java:
     version: oraclejdk8
test:

   post:
     #- mkdir -p $CIRCLE_TEST_REPORTS/cucumber/
     #- find . -type f -regex ".*/target/cucumber/.*cucumber" -exec cp {} $CIRCLE_TEST_REPORTS/cucumber/ \;
     #- mkdir -p $CIRCLE_TEST_REPORTS/webreport/
     #- cp -r ./target/cukes/ $CIRCLE_TEST_REPORTS/webreport/
     - mkdir -p $CIRCLE_ARTIFACTS/unit_test/report/
     - mkdir -p $CIRCLE_TEST_REPORTS/unit_test/report/XML/
     - find . -type f -regex ".*/target/surefire-report/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/unit_test/report/XML/ \;
